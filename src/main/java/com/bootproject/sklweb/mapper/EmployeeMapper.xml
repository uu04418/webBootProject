<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.bootproject.sklweb.mapper.EmployeeMapper">
    
    
    <select id="getEmpById" resultType="com.bootproject.sklweb.entity.Employee">
        SELECT * FROM employee WHERE id=#{id}
    </select>
    
    
     <select id="insertEmployee" parameterType="com.bootproject.sklweb.entity.Employee">
        INSERT INTO employee VALUES (#{id} , #{name}  ,#{email})
    </select>

    
</mapper>